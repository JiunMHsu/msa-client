import Button from '../Button/Button';

const SideBar = () => {
  // const staticOptions = ['', '', '', ''];

  return (
    <div>
      <Button onClick={() => {}}>MENU</Button>
      <Button onClick={() => {}}>HOME</Button>

      <div>{/* boton de explore */}</div>
      <div>{/* boton de collection */}</div>
      <div>{/* seccion de guardados */}</div>
    </div>
  );
};

export default SideBar;
